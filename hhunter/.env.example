# hhunter uses Playwright (headless Chromium) for all data collection —
# no API keys required.  Copy this file to .env and fill in only what you want.

# Optional: set to 1 to always run browsers in headed (visible) mode.
# Same effect as the --headed CLI flag.
# HHUNTER_HEADED=0
